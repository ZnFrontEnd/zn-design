import React from 'react'
import './index.less';

const ContentLayout = ({ children }) => (
    <div className="example-layout-container">
        { children }
    </div>
)

export default ContentLayout
